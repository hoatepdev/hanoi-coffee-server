version: '2'
services:
  hanoi-coffee-api:
    container_name: hanoi-coffee-api
    env_file:
      - .env
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '4000:4000'
    volumes:
      - ./src:/app/src
